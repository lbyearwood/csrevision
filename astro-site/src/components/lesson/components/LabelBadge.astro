---
const { class: className, tone = "default", ...attrs } = Astro.props;
const toneClass = tone === "default" ? "" : `dev-component-label-badge--${tone}`;
const classes = ["dev-component-label-badge", toneClass, className].filter(Boolean).join(" ");
---

<span class={classes} {...attrs}><slot /></span>
