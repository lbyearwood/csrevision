---
const { as: Tag = "p", class: className, tone = "neutral", ...attrs } = Astro.props;
const toneClass = tone === "neutral" ? "" : `dev-component-feedback--${tone}`;
const classes = ["dev-component-feedback", toneClass, className].filter(Boolean).join(" ");
---

<Tag class={classes} {...attrs}><slot /></Tag>
