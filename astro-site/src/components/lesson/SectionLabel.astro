---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<p class="inline-flex rounded-full bg-blue-100 px-4 py-1.5 text-sm font-black uppercase text-[var(--color-primary)]">
  {label}
</p>
