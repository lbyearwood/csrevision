---
import CardShell from "./CardShell.astro";

interface Props {
  title: string;
  class?: string;
}

const { title, class: className } = Astro.props;
---

<CardShell title={title} dataCard="feedback" class={className}>
  <slot />
</CardShell>
