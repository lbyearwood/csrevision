---
import CardShell from "./CardShell.astro";

interface Props {
  title: string;
  class?: string;
}

const { title, class: className } = Astro.props;
const cardClass = ["modelling-card", className].filter(Boolean).join(" ");
---

<CardShell title={title} dataCard="modelling" class={cardClass}>
  <slot />
</CardShell>
