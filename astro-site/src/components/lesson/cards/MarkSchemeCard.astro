---
import CardShell from "./CardShell.astro";

interface Props {
  title: string;
  class?: string;
}

const { title, class: className } = Astro.props;
const cardClass = ["mark-scheme-card", className].filter(Boolean).join(" ");
---

<CardShell title={title} dataCard="mark-scheme" class={cardClass}>
  <slot />
</CardShell>
