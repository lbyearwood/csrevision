---
import CardShell from "./CardShell.astro";

interface Props {
  title: string;
  class?: string;
}

const { title, class: className } = Astro.props;
const cardClass = ["learning-objectives-card", className].filter(Boolean).join(" ");
---

<CardShell title={title} dataCard="learning-objectives" class={cardClass}>
  <slot />
</CardShell>
