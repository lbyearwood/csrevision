---
import CardShell from "./CardShell.astro";

interface Props {
  title: string;
  class?: string;
}

const { title, class: className } = Astro.props;
const cardClass = ["support-card", className].filter(Boolean).join(" ");
---

<CardShell title={title} dataCard="support" class={cardClass}>
  <slot />
</CardShell>
